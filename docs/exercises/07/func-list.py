def count_errors(lines):
    count = 0
    for line in lines:
        if  :
            count += 1
    return count

logs = ["INFO Start", "ERROR Disk full", "INFO OK", "ERROR Timeout"]
print(count_errors(logs))
